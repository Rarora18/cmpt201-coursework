#include <stdio.h>

#include <pthread.h>
#include <unistd.h>
#define MAX 1000000
int cnt = 0;
void *counter_thread(void *arg) {

  for (int i = 0; i < MAX; i++) {
    cnt += 1;
  }
  return NULL;
}
int main() {
  pthread_t thread_id;

  pthread_create(&thread_id, NULL, counter_thread, NULL);

printf("the value of cnt is %d", cnt);
}
