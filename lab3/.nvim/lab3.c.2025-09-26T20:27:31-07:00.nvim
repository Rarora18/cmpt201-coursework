#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>

int main(){

char *line = NULL;
size_t len = 0;

if (getline(&line, &len, stdin) != 1){

printf("you typed %s", line);
}

printf("%s", line);

}
