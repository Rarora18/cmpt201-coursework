#define _POSIX_C_SOURCE 200809L
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  while (true) {

    printf("Enter input: " );

    char *line = NULL;
    size_t len = 0;

    if (getline(&line, &len, stdin) != 1) {

      printf("%s", line);
    }
  }
}
