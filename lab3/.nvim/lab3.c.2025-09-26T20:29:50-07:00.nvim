#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
int main() {
while (true) {
  char *line = NULL;
  size_t len = 0;

  if (getline(&line, &len, stdin) != 1) {

    printf("Enter input: %s", line);
  }

  printf("%s", line);
}
}
